################## ROTATION_PLATFORM_UART ###################

#File
+ uart: the driver of uart communication in pix
+ rotation_platform_uart_main.cpp: the application sending some information through uart
